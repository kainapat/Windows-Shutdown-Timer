# Dropdown Time Selectors for Timer and Clock Modes

Replaced free-form keyboard text entry (`QSpinBox` and `QDateTimeEdit`) with discrete `QComboBox` dropdowns and a `QDateEdit` calendar popup in a unified 3-column layout. This eliminates the need for manual keyboard typing on desktop and touch environments while preventing invalid inputs and preserving visual symmetry across mode transitions.
